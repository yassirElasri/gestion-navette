@model IEnumerable<GestionArticles.Models.Autocar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.maricule)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre_places)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SocietéTransport.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.maricule)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_places)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SocietéTransport.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
